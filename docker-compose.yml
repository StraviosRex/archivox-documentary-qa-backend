services:
  archivox:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - HF_HUB_OFFLINE=1
    volumes:
      - chroma_data:/app/chroma_db

volumes:
  chroma_data:
